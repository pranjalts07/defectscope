data:
  root_dir: "data/raw"
  category: "bottle"
  image_size: 256
  batch_size: 32
  num_workers: 4

cnn:
  architecture: "densenet121"
  num_classes: 2

autoencoder:
  learning_rate: 1.0e-3
  epochs: 50

wandb:
  project: "defectscope"
  entity: null
